name: Waka Readme

on:
  schedule:
    # Runs at 12am IST
    - cron: '30 18 * * *'
  workflow_dispatch:
jobs:
  update-readme:
    name: Update Readme with Metrics
    runs-on: ubuntu-latest
    steps:
      - uses: anmol098/waka-readme-stats@master
        with:
          WAKATIME_API_KEY: ${waka_f16274c1-8f83-4f78-b020-7e7937b9a85b}
          GH_TOKEN: ${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}
